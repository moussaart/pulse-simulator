PyQt5>=5.15.0
numpy>=1.21.0
scipy>=1.7.0
matplotlib>=3.4.0
pyqtgraph>=0.12.0
pyinstaller>=5.0.0


# Optional: GPU acceleration via CUDA (not bundled in the .exe)
# Install manually if you have an NVIDIA GPU with CUDA support:
# pip install cupy-cuda13x   (for CUDA 13.x)
# pip install cupy-cuda12x   (for CUDA 12.x)

navpy>=1.0.1

